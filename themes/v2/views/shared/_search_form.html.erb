<%
  if local_assigns[:url].nil?
    url = search_contributions_path 
  end
%>
<%= form_tag( url, { :method => :get, :id => 'search' } ) do %>
  <%= text_field_tag 'q', query, { :type => 'search', :placeholder => 'search Europeana 1914-1918' } %>
<% end %>

