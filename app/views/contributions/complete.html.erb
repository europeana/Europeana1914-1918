<% @title = t('views.contributions.complete.title') %>

  <p><%= t('views.contributions.complete.p1') %></p>
  
  <div class="link-buttons">
	<%= link_to t('common.links.my_stories'), contributor_dashboard_url %>
  </div>
  
  <%= render :partial => 'progress', :locals => { :step => 5 } %>
