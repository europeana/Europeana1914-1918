<%

  html = ''
  provide :map_google, true
  provide :map_latlng, metadata.fields['location_map']
  provide :map_zoom, metadata.fields['location_zoom']
  provide :map_placename, metadata.fields['location_placename']

	if content_for?( :map_latlng )
		html = content_tag(
			'div',
			content_tag(
				'div',
				content_tag(
					'div',
					'',
					:id => 'map'
				),
				:id => 'map-container'
			),
			:class => 'col-cell'
		)
	end

-%>
<%= html.html_safe -%>
