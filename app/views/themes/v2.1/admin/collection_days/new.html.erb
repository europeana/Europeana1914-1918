<% provide :title, t('views.admin.collection_days.new.title') %>

<div class="section top">
  
  <h2><%= yield :title %></h2>

  <%= render :partial => 'form', :locals => { :collection_day => @collection_day } %>
  
</div>
