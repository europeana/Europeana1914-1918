<% provide :title, t('views.annotations.unflag.title') %>

<div class="section top">
  
  <h2><%= yield :title %></h2>
  
  <p><%= t 'views.annotations.unflag.question' %></p>
  
  <ul>
    <li><strong><%= t 'activerecord.models.attachment' %>:</strong> <%= link_to attachment_title(@annotation.attachment), [ @annotation.attachment.contribution, @annotation.attachment ] %></li>
    <li><strong><%= t 'activerecord.models.annotation' %>:</strong> <%= @annotation.text %></li>
  </ul>

  <%= semantic_form_for @annotation, :url => unflag_annotation_path(@annotation), :html => { :method => :put } do |form| %>
    <%= form.buttons do %>
      <%= redirect_field %>
      <%= form.commit_button :label => t('actions.unflag') %>
      <%= form_cancel_link contribution_attachment_path(@annotation.attachment.contribution, @annotation.attachment) %>
    <% end %>
  <% end %>

</div>
