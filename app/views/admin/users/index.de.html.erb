<% @title = t('views.admin.users.index.title') %>
<p>Auf dieser Seite können Sie neue Nutzer eintragen, Details der Nutzerkonten und Berechtigungen einsehen und bearbeiten oder auch alle Beiträge eines bestimmten Nutzers sehen.</p>

<ul><li>Um einen neuen Nutzer hinzuzufügen, klicken Sie auf „Neuer Nutzer".</li>
<li>Um Nutzerdetails zu bearbeiten oder Berechtigungen zu ändern, klicken Sie auf den Nutzernamen und wählen dann „Nutzerkonto bearbeiten".</li>
<li>Um alle Beiträge eines Nutzers zu sehen, klicken Sie auf den Nutzernamen und wählen dann „Beiträge".</li></ul>

<%= form_tag admin_users_path, { :method => :get } do %>
  <div><%= select_tag 'role', user_role_options_for_select(@role), :title => t('activerecord.attributes.user.role_name') %> <%= submit_tag t('actions.filter'), :name => nil %></div>
<% end %>

<div class="action-links">
<ul>
  <li><%= link_to t('views.admin.users.index.links.new'), new_admin_user_path, :class => 'new-member' %></li>
</ul>
</div>

<% if @users.present? -%>
  <table class="collection-members admins">
    <caption><%= t('views.admin.users.index.caption') %></caption>
    <tr>
      <th scope="col"><%= t 'activerecord.attributes.contact.full_name' %></th>
      <th scope="col"><%= t 'attributes.email' %></th>
      <th scope="col"><%= t 'activerecord.attributes.user.role_name' %></th>
      <th scope="col" colspan="2"><%= t 'views.table.header.actions' %></th>
    </tr>
    <% for user in @users %>
      <tr>
        <td><%= link_to contact_name(user.contact), ['admin', user], :title => t('views.admin.users.index.links.show.title') %></td>
        <td><%= user.email %></td>
        <td><%= user.role.name.titleize %></td>
        <td><%= link_to t('actions.edit'), edit_admin_user_path(user), :class => 'edit-member' %></td>
        <td><%= link_to t('actions.delete'), delete_admin_user_path(user), :class => 'delete-member' %></td>
      </tr>
    <% end %>
  </table>
  <%= will_paginate @users %>
<% else -%>
  <p><%= t 'views.admin.users.index.empty' %></p>
<% end -%>

